import React from 'react';

function SearchBar(){
    return (
        <div>
            <input type='text' placeholder='Enter a song or artist...' />
            <button>SEARCH</button>
        </div>
    );
}

export default SearchBar;